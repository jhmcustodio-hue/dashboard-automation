bundle:
  name: dashboard-automation

include:
  - resources/*.yml

artifacts:
  dashboard_automation:
    type: whl
    build: uv build --wheel
    path: .

targets:
  dev:
    mode: development
    default: true
  prod:
    mode: production
